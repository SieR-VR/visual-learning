import { Center, Text } from '@chakra-ui/react';
import { TensorInputProps } from '../inputs/tensorProps';
import NodeHeader from './NodeHeader';

interface NodeBodyProps {
  id: string;
  inputData: TensorInputProps;
  
}

const NodeBody = ({
  id,
  inputData
}: NodeBodyProps) => {
  return (
    <>
    </>
  )
}